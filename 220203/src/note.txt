CREATE TABLE tmp_02(
    no INT NOT NULL auto_increment PRIMARY KEY,
    username VARCHAR(50) NOT NULL,
    title VARCHAR(50),
    content VARCHAR(5000),
    ofn VARCHAR(100),
    fsn VARCHAR(100),
	view INT,
	recommend INT,
	time VARCHAR(50)
);

INSERT INTO tmp_02 values (default,'user','title','content','ofn','fsn',0,0, now() );

http://localhost:8080/p0203/ping.do

사용 jar 파일